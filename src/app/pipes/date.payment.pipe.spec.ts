import { DatePaymentPipe } from './date.payment.pipe';

describe('DatePaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
